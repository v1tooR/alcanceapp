# Origem dos dados: `simulado` (padrão) usa a base fictícia em memória;
# `api` liga o adaptador HTTP em src/services/http.
VITE_MODO_DADOS=simulado

# Base da API quando VITE_MODO_DADOS=api
VITE_API_URL=/api
